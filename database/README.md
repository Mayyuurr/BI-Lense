# Database Organization & Schema Documentation

PostgreSQL database schemas, migrations, and seed scripts.

## Directory Structure
- `migrations/`: SQL migration files and Alembic revision logs.
- `seed/`: Initial seed data scripts and sample SME synthetic databases.

> **Note:** The final ER database schema will be frozen after the data dictionary and research domain models are approved.
